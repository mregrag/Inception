# Basic Redis configuration for WordPress caching
port 6379
bind 0.0.0.0
protected-mode yes
maxmemory 256mb
maxmemory-policy allkeys-lru
appendonly no
requirepass ${REDIS_PASSWORD}

# Performance tuning
tcp-backlog 511
timeout 0
tcp-keepalive 300
databases 16
save 900 1
save 300 10
save 60 10000
