# 1. Listen on all network interfaces (to accept connection from WordPress)
bind 0.0.0.0

# 2. Run in the foreground (required for Docker)
daemonize no

# 3. Set the data directory for persistence
dir /data/redis

# 4. Set the password by reading it from the Docker secret file
requirepass /run/secrets/redis_password
